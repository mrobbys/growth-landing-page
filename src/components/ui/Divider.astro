---
interface Props {
  bgColor: string;
  borderColor: string;
}

const { bgColor, borderColor } = Astro.props;
---

<div class={`${bgColor}`}>
  <hr class={`mx-auto w-[95%] max-w-7xl ${borderColor}`} />
</div>
